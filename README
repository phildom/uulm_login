A simple shell script to log into the University of Ulm welcome network.

usage: uulm
       uulm -d

Without any option the script logs into the network and -d disconnects
from the network.
In order for the script to work you need to assign your login name to
the variable user.
